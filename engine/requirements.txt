# Azure Functions Python worker (v2 programming model).
azure-functions

# Managed identity for all data-plane access (no keys, no connection strings).
azure-identity

# Cosmos DB: read account history and persist scored transactions (Block 4).
azure-cosmos

# NOTE: the Service Bus topic trigger needs no SDK (its binding ships in the
# host.json extension bundle). pyodbc (case rows in Azure SQL) and, if the
# engine publishes flagged cases itself, azure-servicebus arrive with Block 5.
